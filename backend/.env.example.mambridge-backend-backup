PORT=3001
NODE_ENV=production
JWT_SECRET=replace_with_a_long_random_secret
IVS_DEFAULT_REGION=eu-west-1
AWS_REGION=eu-west-1

# Do not add AWS_ACCESS_KEY_ID or AWS_SECRET_ACCESS_KEY on EC2.
# The backend uses the EC2 instance profile (IVS_EC2_Role).
